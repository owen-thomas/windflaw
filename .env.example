# Copy to .env for local dev. Never required — without it, /api/narration
# reports a failed generation and the client shows its deterministic
# template instead (see api/narration.ts and src/lib/narrate.ts).
ANTHROPIC_API_KEY=
